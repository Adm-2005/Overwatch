/* 

This page allows users to connect their online groups/communities with our embedded wrappers.

Flow:-
User -> Selects a platform (Discord/Telegram/Facebook) -> Connection details form appears based on the selected platform -> Information is sent to Backend -> Wrapper is connected

*/

const Connection = () => {
    return (
        <div className='flex flex-col justify-between min-h-screen'>

        </div>
    );
};

export default Connection;